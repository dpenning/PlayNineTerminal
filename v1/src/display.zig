const std = @import("std");
const Game = @import("game.zig").Game;
const Terminal = @import("terminal.zig").Terminal;

pub fn displayGameInTerminal(
    game: *Game,
    terminal: *Terminal,
) void {}
